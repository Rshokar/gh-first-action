name: First Workflow # The name of the workflow

on:
  workflow_dispatch # on -> tells when a workflow should be activated

jobs:
  first-job: # The name does not matter. But this is a job
    runs-on: ubuntu-latest # defines the environments we can use

    steps:
      - name: Print Greeting # This is a single step. This is
        run: echo "Hello world!"

      - name: Print Goodbye
        run: echo "Done - bye!"

